from hook_common import add_context

add_context('UserPromptSubmit', '[MDF HARNESS] For non-trivial code work: read AGENTS.md, use the feature loop, search learned-recipes, and do not claim PASS without command output + artifacts. Use mdf_* subagents explicitly when needed.')

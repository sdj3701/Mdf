# Project-local Codex config for the MDF harness.
# Project-local hooks only load when this .codex layer is trusted.
project_doc_max_bytes = 65536

[features]
codex_hooks = true

[agents]
max_threads = 6
max_depth = 1
